spring:
  application:
    name: prevent_test

com:
  dane:
    brown:
      scheduled:
        preventer:
          prevent-regex: com.dane.brown.*.test1,com.dane.brown.*.test3
logging:
  level: debug